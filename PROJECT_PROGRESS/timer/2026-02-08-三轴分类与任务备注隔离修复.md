# 进度记录模板

## 时间
- 2026-02-08

## 目标
- 将 Timer 手动模式分类体系切换为“客观三轴分类”（场景/行为/对象），避免继续沿用旧的 log-categories 分类树
- 修复 Project 内 todo 双击打开的“任务备注”与左侧全局“备忘录”联通的问题，实现按 todoId 隔离存储
- 对 AI 解析接口进行最小改造：三轴分类输出 + 标签归一化（去 # / 去 emoji / 去重）

## 改动
- 新增 Timer 专用三轴分类接口：[`project-nexus/app/api/timer-categories/route.ts`](project-nexus/app/api/timer-categories/route.ts:1)
  - 输出固定树：场景(4) → 行为(6) → 对象(10)
  - 不复用 `/api/log-categories`，避免影响其它模块
- Timer 手动分类选择器改为使用新接口：[`timer/src/lib/category-cache.ts`](timer/src/lib/category-cache.ts:58)
  - 请求从 `/api/log-categories` 切换为 `/api/timer-categories`
  - 缓存 key 升级为 `category-cache-v3` 强制刷新旧缓存（避免 7 天 TTL 造成旧分类残留）
- 手动分类 UI 禁用新增/删除分类入口（客观分类固定）：[`timer/src/components/shared/ThreeLayerCategorySelector.tsx`](timer/src/components/shared/ThreeLayerCategorySelector.tsx:24)
- AI parse 规则最小改造：[`project-nexus/app/api/timer-tasks/parse/route.ts`](project-nexus/app/api/timer-tasks/parse/route.ts:9)
  - prompt 强制输出三轴分类格式 `场景/行为/对象`
  - instanceTags 归一化：去 `#`、去 emoji、去重，并与文本中 `#tag` 合并
- 修复任务备注联通：[`timer/src/pages/Memo.tsx`](timer/src/pages/Memo.tsx:10)
  - 兼容 HashRouter 与 search 两种 query 来源
  - 修复 `URLSearchParams.get()` 后二次 `decodeURIComponent` 导致异常并回退为 `manifesto-global`
  - 增加 hashchange 监听，保证 Electron/HashRouter 参数时序稳定
  - 标题栏展示当前 `storageKeyPrefix` 用于快速确认隔离是否生效

